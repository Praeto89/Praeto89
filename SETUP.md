# Praeto89 GitHub Profile

Ein pädagogisches GitHub-Profil mit Canvas-Leinwand-Ästhetik in Pastellfarben.

## 🚀 Setup-Anleitung

### Schritt 1: Repository auf GitHub erstellen

1. Gehe zu [GitHub](https://github.com) und erstelle ein **neues öffentliches Repository**
2. **Wichtig**: Der Repository-Name muss **exakt** `Praeto89` sein (dein GitHub-Username)
3. **Nicht** mit README initialisieren (wir haben bereits eins)
4. Erstelle das Repository

### Schritt 2: Assets hochladen

Bevor du pushst, musst du die Canvas-Textur und Header-Grafik erstellen:

1. **Canvas-Textur** (`assets/canvas-bg.png`):
   - Nutze die angehängte Canvas-Leinwand-Textur
   - Speichere sie als `canvas-bg.png` im `assets/` Ordner
   
2. **Header-Grafik** (`assets/header.png`):
   - Erstelle einen Header (1200x300px)
   - Hintergrund: Canvas-Textur
   - Text: "Martin St - Pedagogy × Technology × Development"
   - Schrift: Sans-Serif
   - Farben: #C7CEEA (Lavender), #8B9DC3 (Soft Blue)
   - Speichere als `header.png` im `assets/` Ordner

3. **Optional - Projekt-Screenshot** (`assets/project-preview.png`):
   - Screenshot deines PresentationTemplate Projekts
   - Größe: 800x450px (16:9)
   - Speichere im `assets/` Ordner

Siehe `assets/CANVAS_UPLOAD_INSTRUCTIONS.md` für Details.

### Schritt 3: Repository zu GitHub pushen

```powershell
# Im Praeto89 Verzeichnis ausführen:
git add .
git commit -m "Initial commit: Pädagogisches Profil mit Canvas-Ästhetik"
git branch -M main
git remote add origin https://github.com/Praeto89/Praeto89.git
git push -u origin main
```

### Schritt 4: Snake Animation aktivieren

1. Gehe zu deinem Repository auf GitHub
2. Klicke auf **Actions** Tab
3. Du solltest den Workflow "Generate Snake Animation" sehen
4. Klicke auf den Workflow und dann auf **"Run workflow"** → **"Run workflow"**
5. Warte ~1 Minute bis der Workflow abgeschlossen ist
6. Der `output` Branch wird automatisch erstellt mit `snake.svg`

### Schritt 5: Snake Animation im README aktivieren

Nach erfolgreichem Workflow-Run:

1. Öffne `README.md`
2. Finde diese Zeilen (ganz unten bei GitHub Stats):
   ```markdown
   <!-- Snake Animation - Will be generated by GitHub Actions -->
   <!-- Uncomment after first successful workflow run -->
   <!-- <img src="https://raw.githubusercontent.com/Praeto89/Praeto89/output/snake.svg" alt="Snake animation" /> -->
   ```
3. Entferne die `<!-- -->` Kommentare:
   ```markdown
   <!-- Snake Animation -->
   <img src="https://raw.githubusercontent.com/Praeto89/Praeto89/output/snake.svg" alt="Snake animation" />
   ```
4. Commit und push die Änderung

### Schritt 6: Optional - Projekt-Screenshot hinzufügen

Wenn du einen Screenshot hast:

1. Finde diese Zeile in `README.md`:
   ```markdown
   <!-- <img src="./assets/project-preview.png" alt="PresentationTemplate Preview" width="600"> -->
   ```
2. Entferne die `<!-- -->` Kommentare:
   ```markdown
   <img src="./assets/project-preview.png" alt="PresentationTemplate Preview" width="600">
   ```
3. Commit und push

## 📋 Checkliste

- [ ] Repository `Praeto89` auf GitHub erstellt (öffentlich)
- [ ] Canvas-Textur als `assets/canvas-bg.png` hochgeladen
- [ ] Header-Grafik als `assets/header.png` erstellt und hochgeladen
- [ ] Alle Dateien zu GitHub gepusht
- [ ] GitHub Actions Workflow manuell gestartet
- [ ] Snake Animation im README aktiviert (nach Workflow-Erfolg)
- [ ] Optional: Projekt-Screenshot hinzugefügt
- [ ] Profil auf https://github.com/Praeto89 überprüft

## 🎨 Farb-Palette (PresentationTemplate)

```css
/* Pastel Colors */
--paper-bg: #fdfbf7;        /* Canvas/Paper Background */
--soft-blue: #8B9DC3;       /* Stats Title */
--light-blue: #A8C5D1;      /* Languages Title */
--lavender: #C7CEEA;        /* Icons */
--mint: #B5EAD7;            /* Language Icons */
--peach: #FFD6BA;           /* Badges */
--text-gray: #6B7280;       /* Text */
--border: #E8DCC4;          /* Borders */

/* Snake Animation Palette */
--snake-blue: #B8D4E8;
--snake-mint: #B5EAD7;
--snake-lavender: #C7CEEA;
--snake-peach: #FFD6BA;
```

## 🔧 Technische Details

- **GitHub README Stats**: Custom-Farben aus PresentationTemplate
- **DevIcon**: Icons für JavaScript, HTML, CSS, Python
- **Shields.io**: LinkedIn-Badges und EdTech-Platform-Badges (flat-style)
- **Snake Animation**: Platane/snk mit Cron-Job (alle 12 Stunden)
- **Canvas-Dividers**: 80% Opacity für subtilen Look

## 📚 Weitere Anpassungen

### LinkedIn-URL anpassen (falls nötig):
Suche in `README.md` nach `martin-stalder-787239216` und ersetze mit deiner LinkedIn-ID.

### EdTech-Badges anpassen:
Die Badges für Lehreroffice, Pupil, Escola kannst du bei [Shields.io](https://shields.io) customizen.

### Farben ändern:
Alle Farb-Codes sind in den URLs der GitHub Stats Cards. Passe die Hex-Werte nach Belieben an.

## ❓ Probleme?

Siehe `TROUBLESHOOTING.md` für häufige Fehler und Lösungen.
